# --- secrets: never commit ---
firmware/credentials.conf
host/.env
*.pem
*.key

# --- west workspace (topdir is the repo root; pinned by firmware/west.yml) ---
/.west/
/zephyr/
/modules/
/bootloader/
/tools/

# --- build output ---
build/
# Side-by-side west build dirs, e.g. build-rxonly/ for the listen-only bring-up variant.
build-*/
twister-out*/
enclosure/stl/
__pycache__/
*.pyc
.venv/
.pytest_cache/

# --- KiCad ---
*-backups/
*.kicad_prl
*.kicad_sch-bak
*.kicad_pcb-bak
fp-info-cache
~*.*
*.net

# --- mosquitto persistence, if a broker is run from inside the repo ---
mosquitto.db

# --- session transcript archive (SpecStory extension) ---
.specstory/

# --- git worktrees, when a second session works on its own branch (EnterWorktree) ---
.claude/worktrees/

# --- macOS ---
.DS_Store
